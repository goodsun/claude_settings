# 案件一覧表示案件ID観察用ツール

## 概要
該当の案件一覧に表示されている案件IDを表示順に.txtファイルにて保存する。
実行すると自動で作成されるstoreディレクトリ内に保存する。
store内に`YYYY-MM-DD`のディレクトリが作成されて、その中に.txtが作成される。

## 環境設定
以下のコマンドを実行し、パッケージを一括インストールする

`pip3 install -r requirements.txt`

## 実行方法
本ディレクトリに移動して以下のコマンドを実行
python3 main.py
